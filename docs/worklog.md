# 作業記録

## 2026-07-14 (作り直し初日)

### やったこと

1. **現状調査と方針決定**
   - 旧アプリのコードはどこにも現存しないことを確認(GitHubリポジトリは空、Drive/Artifactにもなし)
   - 要件を整理: パラメータ入力で木製スプーンの3Dモデルを生成 / 汎用3Dデータ出力 / 講座参加者もスマホで使う
   - 旧アプリの失敗原因(形がスプーンらしくない)を分析し、`docs/redesign-plan.md` に方針を文書化
   - ChatGPT(コーデックス)並走用のプロンプトも同内容で作成

2. **Phase 1 実装**
   - 形状エンジン `src/spoon.ts`: プリミティブ合成ではなく、背骨+平面輪郭+断面モーフィングのロフト方式で1本の連続曲面を生成
   - パラメータ10種(作り手の言葉: 匙面の長さ/幅/深さ、首の位置/太さ、クランク角など)+柄端の形3種
   - プリセット4種(ティー/デザート/サーバー/杓子)、共有URL、スマホ対応

3. **GitHub Pages 公開**
   - Actions ワークフローで `gh-pages` ブランチへ自動デプロイ(push のたびに反映)
   - 公開URL: https://shinsuke75.github.io/spoon-app/
   - 注: Actions トークンでは Pages を有効化できず、Settings→Pages の初回設定のみ手動で実施済み

4. **コーデックス版のレビューと統合**
   - コーデックス版を実際にビルド・起動して評価
   - **UIデザインはコーデックス版が優秀 → 全面採用**(「木の匙」ブランド、ビュー切替、グループ化されたパネル、ガイド線表示、自動フレーミング)
   - **形状エンジンはコーデックス版に問題**(輪郭のカクつき、匙先のつまみ、柄端が杭状、縁がナイフエッジ)**→ Claude版を維持**
   - 機能(プリセット/書き出し/共有)は Claude 版を新UIに統合

5. **フィードバック対応**
   - サイドビューの首〜柄のうねり: クランクの立ち上がりを smoothstep 積分に変更、皿深さカーブの端の傾きを0に修正
   - 書き出しを .3dm 案から**汎用形式(STL / OBJ / GLB)に方針変更**して実装(動作確認済み)
   - スプーンが画面下に寄って表示されるレイアウト問題を修正

### 検証方法(再現用)

- `npm run build` → `npx vite preview` → Playwright + headless Chromium でスクリーンショット撮影・目視確認
- プリセット切替とスライダー極値で形状破綻がないことを確認
- 書き出しは実ダウンロードでファイル内容を検証(STLの三角形数整合、OBJ/GLBのサイズ)

### 未解決・次回やること

- **【最優先】サイドビューがまだ気に入らない(ユーザーフィードバック)**
  - うねり自体は解消したが、プロファイルの「印象」が不満とのこと。具体的にどこが不満かは未ヒアリング
  - 候補: 匙面の底の丸みの出方 / 首の谷の深さ・位置 / 柄が直線的すぎる(実物は緩やかに反り続けることが多い) / 匙先の上がり
  - 次回: 実物のスプーン(講座の型紙 .3dm / .ai が Drive にある)の側面プロファイルと重ねて比較し、どこが違うかを特定してから直す。側面図を2D線で重ね表示する比較モードを作ると議論しやすいはず
- Phase 2 残り: 実寸型紙(SVG/PDF)出力、プリセットの実測キャリブレーション
- 匙面の掘り込みを3Dで再現するか、外形+ガイド線で足りるかは未決定

### 現在の構成

- Vite + TypeScript + Three.js の静的サイト(`src/spoon.ts` = 形状、`src/main.ts` = UI/3D表示、`src/params.ts` = パラメータ定義)
- ブランチ: `claude/spoon-app-redesign-h12jap` / デプロイ: push で自動
- コーデックス版一式はチャット添付の zip(スクリーンショット・レビュー依頼文書含む)
